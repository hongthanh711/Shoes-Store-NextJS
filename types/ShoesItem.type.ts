type ShoesItemType = {
    id: string
    title: string
    subTitle: string
    desr: string
    price: number
    srcImg: string
}

type SizeType = {
    size: string
    enabled: boolean
}

export type { ShoesItemType, SizeType }
